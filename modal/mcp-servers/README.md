# MCP Servers

Place Modal / remote **Model Context Protocol** server deployments here.

Planned integrations:
- Filesystem / Cabinet MCP
- Browser MCP bridge (wraps `browser_automation.py`)
- Supabase / memory MCP

For Phase 8, browser automation and Tavily search are wired as native agent tools
(`browser_action`, `web_search`) rather than full MCP protocol servers.
