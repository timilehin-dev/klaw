# Document Generation Skill

This skill allows the agent to generate industry-standard documents (PDF, DOCX, XLSX, CSV, JSON).
It relies on separating content generation (LLM) from file rendering (Python script in Modal Sandbox).
